
export const initialStateAuth = {};

const authState = (state = initialStateAuth, action: {type: string, payload: any}): any => {
    switch(action.type) {
        case "212":
            break;
        default:
            return state;
    }
};

export  default authState;
